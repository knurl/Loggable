A very simple macro for adding an os.Logger to your class, struct or actor, which:
- puts your bundle name into the subsystem (which is what most people would want)
- places the class/struct/actor name into the category section of the Logger

Just add @Loggable to your class/struct/actor definition! Easy.
